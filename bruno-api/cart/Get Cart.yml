info:
  name: Get Cart
  type: http
  seq: 1

http:
  method: GET
  url: "{{Local_URL}}cart"
  auth:
    type: bearer
    token: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
